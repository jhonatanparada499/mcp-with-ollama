services:
    app:
       # build:
       #     context: ./app
       # expose:
       #     -  "5000"
       # restart: always
    nginx: 
        image: frrouting/frr:v8.4.0
        # volumes:
        # - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
        # depends_on:
        # - app
        # ports:
        # - "80:80"
        restart: always

